import * as React from "react";
import { Formik, Field, Form, ErrorMessage } from "formik";
import * as Yup from "yup";

export const Register=()=>{
    return<>
        <div>
            <h2>Registrace</h2>
            <p>Nejakej text a formulář</p>
        </div>
    </>;
}